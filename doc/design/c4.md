flowchart TB
subgraph L1["Level 1: System Context"]
User["👤 Audit Staff"]
System["🏢 Evan AI Audit Assistant"]
GitHub["🐙 GitHub Copilot"]

        User -->|"Upload documents<br/>Get AI answers"| System
        System -->|"OAuth & API calls"| GitHub
    end

------------------------------------------------
flowchart TB
subgraph L2["Level 2: Container Diagram"]
subgraph WebApp["🌐 Spring Boot Application"]
FE["🖥️ Frontend<br/>(Thymeleaf + HTMX)"]
BE["⚙️ Backend<br/>(Spring MVC)"]
AI["🤖 AI Layer<br/>(Spring AI)"]
DB["💾 Storage<br/>(JSON Files)"]
end

        User["👤 User"] -->|"HTTPS"| FE
        FE -->|"REST API"| BE
        BE -->|"Chat/Embed"| AI
        AI -->|"Read/Write"| DB
        AI -->|"API Calls"| Copilot["✨ GitHub Copilot API"]
    end
